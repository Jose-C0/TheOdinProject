<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
  <% messages.forEach(item=> { %>
    <dl>
      <dt><%= item.user %></dt>
      <dd><%= item.text %></dd>
      <dd><%= item.added %></dd>
      </dl>
    <% }) %>
</body>
</html>